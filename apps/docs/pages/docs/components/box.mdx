import { Box } from 'mayumi/box'
import { Playground } from '@/components/playground'
import { Layout } from '@/components/layouts'

# Box

The Box is used to contain everything with border.

## Default

<Playground code={`
<Box
  css={{
    w: '$20',
    h: '$20',
  }}
/>
`
}/>

## Border less

<Playground code={`
<Box
  bordered={false}
  css={{
    w: '$20',
    h: '$20',
  }}
/>
`
}/>

export default ({ children }) => (
  <Layout>
    {children}
  </Layout>
);